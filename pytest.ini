[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    dev: tests for the dev branch (permissive thresholds)
    stg: tests for the staging branch (tighter thresholds)
    prod: tests for the production branch (strictest thresholds)
addopts = --tb=short
